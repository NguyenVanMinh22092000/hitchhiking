import { borderRadiuses, widths } from "@styles/theme";

export const styles = (theme) => ({
    container : {
        width: widths.oneHundred,
        overflowX: "auto",
        borderRadius: borderRadiuses.main,
    }
});